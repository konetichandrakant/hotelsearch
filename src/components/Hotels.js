import React from 'react'

function Hotels(props) {
  const { address,
    checkIn,
    checkOut,
    imageUrl,
    name,
    price,
    rating } = props;

  return (
    <div>
      
    </div>
  )
}

export default Hotels